subdir('proxy')

if enable_gdu
  subdir('gdu')
endif

if enable_udisks2
  subdir('udisks2')
endif

if enable_gphoto2
  subdir('gphoto2')
endif

if enable_afc
  subdir('afc')
endif

if enable_mtp
  subdir('mtp')
endif

if enable_goa
  subdir('goa')
endif
